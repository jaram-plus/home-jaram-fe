/**
 * Login page assets — re-exported from the design-system asset folder so the
 * bundler fingerprints them and views import by name (no string paths).
 *
 * Path alias `@` → `src`. If you don't use it, swap for a relative path:
 *   '../../design-system/assets/logos/jaram-mark.png'.
 */

import jaramMark from '@/design-system/assets/logos/jaram-mark.png';

export { jaramMark };
