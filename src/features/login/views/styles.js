/**
 * Shared inline-style constants for the login views (kept out of parts.jsx so
 * that file only exports components — see react-refresh/only-export-components).
 */

export const blockBtn = { width: '100%', justifyContent: 'center' };
