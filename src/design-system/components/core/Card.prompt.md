**Card** — a warm paper surface with a soft shadow for grouping content.

```jsx
<Card interactive>
  <h3>학술 세미나</h3>
  <p>매주 진행되는 정기 세미나…</p>
</Card>
```

- `interactive` adds a 4px hover lift + deeper shadow (use for clickable cards).
- `padding`: `sm` / `md` / `lg`.
- 상단 빨강 룰은 없다. 카드·모달에 빨간 강조 줄을 두지 않는다 — 구분은 테두리와 여백이 맡는다.
